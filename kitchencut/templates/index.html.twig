{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
    <h1>Home</h1>
    <hr>
    <ol>
        <li>
            <span class="badge badge-success">Done</span> <strong>Create an Application that connects to MySQL</strong><br />
        </li>
        <li>
            <span class="badge badge-success">Done</span> <strong>Create a routine that receive: Range Date, Status and Location (The fields must be optional to the final user) and return the list of invoices with the following information: Location name, date, status and total invoice value.</strong><br />
            <a class="btn btn-link" href="{{ path('invoice_header_index') }}">Example</a>
        </li>
        <li>
            <span class="badge badge-success">Done</span> <strong>Create a routine that receive the location id and return the sum of invoice values grouped by status.</strong><br />
            <a class="btn btn-link" href="{{ path('location_index') }}">Click the eye icon on one of the items listed here</a>
        </li>
        <li>
            <span class="badge badge-success">Done</span> <strong>Create a simple list page to show the result</strong><br />
        </li>
        <li>
            <span class="badge badge-success">Done</span> <strong>Create a MySQL query that return the quantity of Lines by Invoice (It is not necessary link this query to your sourcecode)</strong><br />
            <a class="btn btn-link" href="{{ path('invoice_header_index') }}">Example</a>
        </li>
    </ol>

    <p><a href=https://github.com/iampedropiedade/kitchencut">Git repository</a></p>
{% endblock %}
